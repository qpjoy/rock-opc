name: ${COMPOSE_PROJECT_NAME:-opc-elk-ha}

x-elastic-common: &elastic-common
  image: docker.elastic.co/elasticsearch/elasticsearch:${ELASTIC_VERSION:-8.13.4}
  restart: unless-stopped
  environment: &elastic-common-env
    cluster.name: opc-elk-ha
    xpack.security.enabled: "false"
    xpack.license.self_generated.type: basic
    ES_JAVA_OPTS: -Xms${ELASTICSEARCH_HEAP:-1g} -Xmx${ELASTICSEARCH_HEAP:-1g}
    TZ: ${TZ:-Asia/Shanghai}
    discovery.seed_hosts: es-master01,es-master02,es-master03,es-hot01,es-hot02,es-warm01,es-coord01
    cluster.initial_master_nodes: es-master01,es-master02,es-master03
  ulimits:
    memlock:
      soft: -1
      hard: -1
  networks:
    - elk_ha
  volumes:
    - ../config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
    - elasticsearch_snapshots:/usr/share/elasticsearch/snapshots

services:
  elk-volume-init:
    image: busybox:1.36
    user: root
    restart: "no"
    command:
      - sh
      - -ec
      - |
        mkdir -p /snapshots
        chown -R 1000:0 /snapshots
        chmod -R ug+rwX /snapshots
    volumes:
      - elasticsearch_snapshots:/snapshots
    networks:
      - elk_ha

  es-master01:
    <<: *elastic-common
    environment:
      <<: *elastic-common-env
      node.name: es-master01
      node.roles: master
    depends_on:
      elk-volume-init:
        condition: service_completed_successfully
    volumes:
      - ../config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
      - master01_data:/usr/share/elasticsearch/data
      - elasticsearch_snapshots:/usr/share/elasticsearch/snapshots

  es-master02:
    <<: *elastic-common
    environment:
      <<: *elastic-common-env
      node.name: es-master02
      node.roles: master
    depends_on:
      elk-volume-init:
        condition: service_completed_successfully
    volumes:
      - ../config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
      - master02_data:/usr/share/elasticsearch/data
      - elasticsearch_snapshots:/usr/share/elasticsearch/snapshots

  es-master03:
    <<: *elastic-common
    environment:
      <<: *elastic-common-env
      node.name: es-master03
      node.roles: master
    depends_on:
      elk-volume-init:
        condition: service_completed_successfully
    volumes:
      - ../config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
      - master03_data:/usr/share/elasticsearch/data
      - elasticsearch_snapshots:/usr/share/elasticsearch/snapshots

  es-hot01:
    <<: *elastic-common
    environment:
      <<: *elastic-common-env
      node.name: es-hot01
      node.roles: data_hot,data_content,ingest
    depends_on:
      elk-volume-init:
        condition: service_completed_successfully
    volumes:
      - ../config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
      - hot01_data:/usr/share/elasticsearch/data
      - elasticsearch_snapshots:/usr/share/elasticsearch/snapshots

  es-hot02:
    <<: *elastic-common
    environment:
      <<: *elastic-common-env
      node.name: es-hot02
      node.roles: data_hot,data_content,ingest
    depends_on:
      elk-volume-init:
        condition: service_completed_successfully
    volumes:
      - ../config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
      - hot02_data:/usr/share/elasticsearch/data
      - elasticsearch_snapshots:/usr/share/elasticsearch/snapshots

  es-warm01:
    <<: *elastic-common
    environment:
      <<: *elastic-common-env
      node.name: es-warm01
      node.roles: data_warm
    depends_on:
      elk-volume-init:
        condition: service_completed_successfully
    volumes:
      - ../config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
      - warm01_data:/usr/share/elasticsearch/data
      - elasticsearch_snapshots:/usr/share/elasticsearch/snapshots

  es-coord01:
    <<: *elastic-common
    environment:
      <<: *elastic-common-env
      node.name: es-coord01
      node.roles: ""
    depends_on:
      elk-volume-init:
        condition: service_completed_successfully
    ports:
      - "${ELASTICSEARCH_PORT:-9200}:9200"
    volumes:
      - ../config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
      - coord01_data:/usr/share/elasticsearch/data
      - elasticsearch_snapshots:/usr/share/elasticsearch/snapshots

  kibana:
    image: docker.elastic.co/kibana/kibana:${ELASTIC_VERSION:-8.13.4}
    restart: unless-stopped
    environment:
      ELASTICSEARCH_HOSTS: http://es-coord01:9200
      SERVER_PUBLICBASEURL: http://localhost:${KIBANA_PORT:-5601}
      TZ: ${TZ:-Asia/Shanghai}
    ports:
      - "${KIBANA_PORT:-5601}:5601"
    mem_limit: ${KIBANA_MEMORY_LIMIT:-1g}
    volumes:
      - ../config/kibana.yml:/usr/share/kibana/config/kibana.yml:ro
    depends_on:
      - es-coord01
    networks:
      - elk_ha

  logstash:
    image: docker.elastic.co/logstash/logstash:${ELASTIC_VERSION:-8.13.4}
    restart: unless-stopped
    environment:
      LS_JAVA_OPTS: -Xms${LOGSTASH_HEAP:-512m} -Xmx${LOGSTASH_HEAP:-512m}
      TZ: ${TZ:-Asia/Shanghai}
    ports:
      - "${LOGSTASH_BEATS_PORT:-5044}:5044"
      - "${LOGSTASH_TCP_JSON_PORT:-5000}:5000"
    volumes:
      - ../config/logstash.yml:/usr/share/logstash/config/logstash.yml:ro
      - ../pipeline:/usr/share/logstash/pipeline:ro
    depends_on:
      - es-coord01
    networks:
      - elk_ha

  filebeat:
    image: docker.elastic.co/beats/filebeat:${ELASTIC_VERSION:-8.13.4}
    restart: unless-stopped
    user: root
    command: ["filebeat", "-e", "--strict.perms=false"]
    ports:
      - "${FILEBEAT_HTTP_PORT:-5066}:5066"
    volumes:
      - ../config/filebeat.yml:/usr/share/filebeat/filebeat.yml:ro
      - ../samples/logs:/var/log/opc:ro
      - filebeat_data:/usr/share/filebeat/data
    depends_on:
      - logstash
    networks:
      - elk_ha

  elk-setup:
    image: node:20-alpine
    restart: "no"
    working_dir: /bootstrap
    command:
      - node
      - /bootstrap/1.bootstrap-elastic.js
    environment:
      ELASTICSEARCH_URL: http://es-coord01:9200
      ILM_POLICY_NAME: opc-logs-policy-ha
      ILM_POLICY_FILE: /assets/ilm-policy-ha.json
      INDEX_TEMPLATE_NAME: opc-logs-template-ha
      INDEX_TEMPLATE_FILE: /assets/index-template-ha.json
      SNAPSHOT_REPOSITORY_NAME: opc_snapshots
      SNAPSHOT_REPOSITORY_FILE: /assets/snapshot-repository.json
    volumes:
      - ../assets:/assets:ro
      - ../node_scripts:/bootstrap:ro
    depends_on:
      - es-coord01
    networks:
      - elk_ha

  elk-snapshot-scheduler:
    image: curlimages/curl:8.7.1
    restart: unless-stopped
    command:
      - /bin/sh
      - -ec
      - |
        es_url="$${ELASTICSEARCH_URL:-http://es-coord01:9200}"
        repo="$${SNAPSHOT_REPOSITORY:-opc_snapshots}"
        interval="$${SNAPSHOT_INTERVAL_SECONDS:-86400}"
        retention="$${SNAPSHOT_RETENTION_COUNT:-7}"
        echo "[elk-snapshot-scheduler] waiting for elasticsearch at $${es_url}"
        until curl -fsS "$${es_url}/_cluster/health" >/dev/null 2>&1; do
          echo "[elk-snapshot-scheduler] elasticsearch not ready yet, retry in 5s"
          sleep 5
        done
        echo "[elk-snapshot-scheduler] elasticsearch is reachable, using repository $${repo}"
        while true; do
          snapshot_id="auto-$(date +%Y%m%d%H%M%S)"
          echo "[elk-snapshot-scheduler] creating snapshot $${snapshot_id}"
          if curl -fsS -X PUT "$${es_url}/_snapshot/$${repo}/$${snapshot_id}?wait_for_completion=true" >/dev/null; then
            echo "[elk-snapshot-scheduler] snapshot $${snapshot_id} created successfully"
            snapshots="$$(curl -fsS "$${es_url}/_cat/snapshots/$${repo}?h=id&s=id" || true)"
            count="$$(printf '%s\n' "$${snapshots}" | sed '/^$$/d' | wc -l | tr -d ' ')"
            echo "[elk-snapshot-scheduler] repository $${repo} currently has $${count} snapshots, retention=$${retention}"
            if [ "$${count}" -gt "$${retention}" ]; then
              delete_count=$$((count - retention))
              echo "[elk-snapshot-scheduler] pruning $${delete_count} old snapshots"
              printf '%s\n' "$${snapshots}" | sed '/^$$/d' | head -n "$${delete_count}" | while IFS= read -r old_snapshot_id; do
                [ -z "$${old_snapshot_id}" ] && continue
                echo "[elk-snapshot-scheduler] deleting snapshot $${old_snapshot_id}"
                if ! curl -fsS -X DELETE "$${es_url}/_snapshot/$${repo}/$${old_snapshot_id}" >/dev/null; then
                  echo "[elk-snapshot-scheduler] failed to delete snapshot $${old_snapshot_id}, will retry on next cycle"
                fi
              done
            fi
          else
            echo "[elk-snapshot-scheduler] snapshot $${snapshot_id} failed, likely repository permission or repository state issue; will retry after sleep"
          fi
          echo "[elk-snapshot-scheduler] sleeping $${interval}s before next run"
          sleep "$${interval}"
        done
    environment:
      ELASTICSEARCH_URL: http://es-coord01:9200
      SNAPSHOT_REPOSITORY: opc_snapshots
      SNAPSHOT_INTERVAL_SECONDS: ${SNAPSHOT_INTERVAL_SECONDS:-86400}
      SNAPSHOT_RETENTION_COUNT: ${SNAPSHOT_RETENTION_COUNT:-7}
    depends_on:
      - elk-setup
    networks:
      - elk_ha

networks:
  elk_ha:
    driver: bridge

volumes:
  master01_data:
  master02_data:
  master03_data:
  hot01_data:
  hot02_data:
  warm01_data:
  coord01_data:
  elasticsearch_snapshots:
  filebeat_data:
